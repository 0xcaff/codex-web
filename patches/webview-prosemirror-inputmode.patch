--- a/webview/assets/use-model-settings-BBeDroPt.js
+++ b/webview/assets/use-model-settings-BBeDroPt.js
@@ -62057,6 +62057,11 @@
   let o = new EventTarget(),
     s = new wR(),
     c = a,
+    lre = !1,
+    ure = () => (lre ? {} : { inputmode: `none` }),
+    dre = (e) => {
+      lre !== e && ((lre = e), l.isDestroyed || l.setProps({ attributes: ure }));
+    },
     l = new sL(null, {
       state: jse.create({
         schema: WN,
@@ -62134,6 +62139,18 @@
         let t = l.state.apply(e);
         (l.updateState(t), o.dispatchEvent(new Event(jj)));
       },
+      attributes: ure,
+      handleDOMEvents: {
+        mousedown(e, t) {
+          return dre(!0), !1;
+        },
+        touchstart(e, t) {
+          return dre(!0), !1;
+        },
+        blur(e, t) {
+          return dre(!1), !1;
+        },
+      },
       handlePaste(e, t) {
         if (t.defaultPrevented) return !0;
         let n = t.clipboardData,
