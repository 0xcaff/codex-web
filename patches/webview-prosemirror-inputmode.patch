--- a/webview/assets/prompt-editor-BFzgDIHO.js
+++ b/webview/assets/prompt-editor-BFzgDIHO.js
@@ -24273,6 +24273,13 @@
   let o = new EventTarget(),
     s = new kd(),
     c = a,
+    inputModeDisabled = !1,
+    getAttributes = () => (inputModeDisabled ? {} : { inputmode: `none` }),
+    setPointerInputMode = (e) => {
+      inputModeDisabled !== e &&
+        ((inputModeDisabled = e),
+        l.isDestroyed || l.setProps({ attributes: getAttributes }));
+    },
     l = new wu(null, {
       state: pr.create({
         schema: Od,
@@ -24355,6 +24362,18 @@
         let t = l.state.apply(e);
         (l.updateState(t), o.dispatchEvent(new Event(br)));
       },
+      attributes: getAttributes,
+      handleDOMEvents: {
+        mousedown(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        touchstart(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        blur(e, t) {
+          return (setPointerInputMode(!1), !1);
+        },
+      },
       handlePaste(e, t) {
         if (t.defaultPrevented) return !0;
         let n = t.clipboardData,
