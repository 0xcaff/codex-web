--- a/webview/assets/index-Dcyrtuk-.js
+++ b/webview/assets/index-Dcyrtuk-.js
@@ -151665,6 +151665,18 @@
         }),
       [e],
     ),
+    (0, Z.useEffect)(() => {
+      let t = () => {
+        e.set(wu, !1);
+      };
+      return (
+        (window.__ELECTRON_SHIM__.closeSidebar = t),
+        () => {
+          window.__ELECTRON_SHIM__.closeSidebar === t &&
+            delete window.__ELECTRON_SHIM__.closeSidebar;
+        }
+      );
+    }, [e]),
     (0, Z.useEffect)(() => {
       let a = n.map((n) => {
         let a = e.get(Ah, n.key);
